#include "main.h"

using namespace okapi;

void rightAuton()
{
  /**
  twoBarLift.moveVelocity(twoBarPID(-340));
  translatePID(-48, -48);
  pros::delay(500);
  twoBarLift.moveVelocity(twoBarPID(-100));
  **/
  rotatePID(90);
}
